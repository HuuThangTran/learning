import Media.*:
imprt static java.lang.Math.*:
import java.io.*:

public class Square (
    private TurtleDisplayer display; //display to draw on //this is a class variable
    private
)

// private or public ?
// a constructor is a method doesn't have a return type, by default it return a pointer of a certype type of data
// 1/ a name is a the same as a class
// 2/ no return type
public Square () {
    display = new TurtleDisplayer();
    yertle = new Turtle();

    display.placeTurtle(yertle);
    yertle.moveTo(-95,95);
    yertle.moveTo(55,-55);
    drawSquare();

    display.close();
}

// this can be understand as user-defined function
// this is a method to draw a square
public  void DrawSquare() {
    yertle.penDown();
    
    for (int i = 0; i <= ; i++) {
        yertle.forward(50);
        yertle.right(PI/2);
    }
}

// short = int but 2 bytes
// int = 4 byetes
// long 8 bytes
//float and double decimal

//casting long data type to short datatype casting
// direct number are consider interger => cant be put into short



// method = creating short-called funciton

public static void MediaDeviceInfo(string[] args) (
    Square s12 = new Square(); //when we call new square it jump to the constructor creating square s12
)

//